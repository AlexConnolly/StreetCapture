__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# YOLO weights (auto-downloaded on first run)
*.pt

# Local memory layer (artifact db + images) and legacy v0.1 store
/artifacts/
/data/

# Local camera launcher — holds RTSP credentials, never commit
/run_camera.bat
/run_server.bat
/camera.env

# Web frontend build artifacts
web/node_modules/
web/dist/
web/.vite/
