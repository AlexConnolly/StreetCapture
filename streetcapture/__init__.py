"""StreetCapture — a dual-speed perception engine.

Fast loop  = what is happening now   (live detection + tracking, ~5 FPS)
Slow loop  = what does it mean over time (track aggregation + events, ~1-2 FPS)
"""

__version__ = "0.1.0"
